name: Change ArgoCD Service

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Login to Azure
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_PIPELINE1 }}

      - name: Connect to AKS cluster
        run: |
          az aks get-credentials \
            --resource-group myResourceGroup-new \
            --name azuredevops \
            --overwrite-existing

      - name: Install helm chart
        uses: KazW/helm-github-action@v0.1.0
        with:
          command: install
          args: /nginx-app/chart1
